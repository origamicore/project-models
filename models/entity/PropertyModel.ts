import DecoratorModel from "../DecoratorModel";

export default class PropertyModel
{
    decorator:DecoratorModel;
    name:string;
    description:string;
    type:string;
    defaultValue:string;
    isArray:boolean;
}